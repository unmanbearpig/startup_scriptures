- if admin_signed_in?
  .row
    .col-md-3
      %h4
        = link_to 'New Category', new_category_path, class: 'btn btn-default'

.categories-grid
- categories.each do |category|
  = render category
