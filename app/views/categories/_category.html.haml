.col-lg-3.col-md-4.col-sm-6.col-xs-12.category-grid-item{id: "category-#{category.id}", 'data-category-id' => category.id}
  - if user_signed_in?
    .category-handle
  .thumbnail
    .caption
      = link_to category do
        %h3.category-title= category.name
      %p
        %ul.links-list
          - category.links.each do |link|
            %li
              = render link
