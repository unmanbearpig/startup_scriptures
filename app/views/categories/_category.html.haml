.col-md-3.category-grid-item
  .thumbnail
    - if user_signed_in?
      .grab-circle
    .caption
      = link_to category do
        %h3.category-title= category.name
      %p
        %ul.links-list
          - category.links.each do |link|
            %li
              = render link
